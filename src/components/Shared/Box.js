import styled from 'styled-components';
import {
  color,
  layout,
  space,
  border,
  shadow,
  flexbox,
  grid,
} from 'styled-system';

export const Box = styled('div')(
  color,
  layout,
  space,
  border,
  shadow,
  flexbox,
  grid
);
